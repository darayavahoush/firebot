# Training-only dependencies for firebot.voice_intent -- not needed by the
# console backend/frontend at runtime, only by the scripts in this folder
# and by whatever wires infer.IntentClassifier into server.py later.
torch>=2.1
transformers>=4.40
soundfile>=0.12
librosa>=0.10
numpy>=1.24
pyttsx3>=2.90       # synth_data.py only -- offline TTS via the OS's own engine
